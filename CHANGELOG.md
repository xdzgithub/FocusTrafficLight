# Changelog

## v5.0.1 - 2026-09-18

修复 `Cmd+W` 关闭窗口后聚焦偏慢的问题。

- 修复：关闭窗口后聚焦偏慢（Chrome 中位约 270ms，且约 19% 直接超时跳过）。原因是关闭判定把「AX 窗口列表为空」与「AX 读取失败」混为一谈，两者都会落到 CoreGraphics 屏幕合成列表兜底，而该信号要等到约 569ms（AX 窗口列表只需约 140ms）。于是「关闭应用的最后一个窗口」——正是必须切换焦点的那种情况——每次都走慢路。现区分两者：列表为空即判定窗口已消失，只有读取失败才回落屏幕列表。实测 Finder 关闭 106–118ms、系统设置 135–149ms、Chrome 256–273ms 且不再超时
- 修复：未能解析出窗口号的关闭（日志中 `window=?`）原本要求 AX 列表变空才判定，应用若还有其他窗口则必然等到超时；现改为凭 AX 窗口数下降判定
- 修复：窗口数基线读取失败时，「凭数量下降判定」的条件永远不成立（相当于拿当前值和自身比较），必然等到超时。现改为基线不可用时回退到屏幕列表判定
- 优化：移除应用隐藏路径上手动写的 50ms 延迟。该延迟没有测量依据（由 75ms 改来、无新测量），且与代码自身注释矛盾：它声称「等窗口数更新后再读」，但基线正是要在窗口消失**之前**尽早读取；先等 50ms 反而可能读到已经下降的值，使「数量下降」判定永不成立，把本该立即判定「应用还有别的窗口」的情形拖成 1.2s 超时。它声称要防的「通知早于计数更新」情形，会被读到偏高的旧值（判定为继续等待）并在 15ms 后自动重试，无需延迟。实测隐藏墙钟由 94–106ms 降至 45–61ms
- 优化：仅在确实需要比较窗口数的触发类型（应用隐藏、以及未解析出窗口号的关闭）才读取该基线，最小化与已知窗口号的关闭不再多花一次 AX 查询
- 改进：每次判定都会记录所用的信号来源（`accessibility-list-empty`、`accessibility-count-zero`、`window-absent-from-list`、`minimized-flag`、`on-screen-list` 等）。此前多种不同情形产生的日志文本完全相同，导致慢路径无法被度量，这也是它长期未被发现的原因
- 改进：区分 `kAXUIElementDestroyedNotification`（元素销毁）与 `kAXApplicationHiddenNotification`（应用隐藏），并给前者单独设置 400ms 的等待上限（后者仍为 1.2s）。该通知不一定代表「窗口」被销毁——应用销毁子元素时也会发送（Chrome、腾讯会议均如此），且送达的元素已失效无法辨识，只能靠「AX 窗口数是否下降」判断。实测真正的窗口销毁在窗口数上中位约 37ms、最差约 230ms 就反映出来，因此超过 400ms 仍未变化基本可判定为子元素噪音。此前这类触发有约三分之一要等满 1.2s 才放弃，期间每 15ms 在主线程做一次 AX 读取。实测调整后此类触发上限生效：超时耗时 458ms（此前 1.2s+），而真实窗口销毁仍为 45–69ms
- 说明：Chrome 关闭约 260ms 是 Chrome 自身把窗口保留在 AX 列表中的时长（实测 273–674ms，屏幕列表更晚），即其应用侧的上报延迟，非本程序的额外开销

## v5.0.0 - 2026-09-17

macOS 27 适配版本。功能行为与 v4.x 保持一致，但底层实现换成公开 API —— macOS 27 移除了 v4.x 依赖的几个私有接口。

- 修复：macOS 27 上窗口身份无法识别。私有 AX 属性 `AXCGWindowID` 已被移除（实测返回 `kAXErrorAttributeUnsupported` / `kAXErrorNoValue`），导致目标窗口号恒为空、「目标窗口是否已消失」检查永久短路。改用公开的 `NSWindow.windowNumbers(options: [.allApplications])` 获取窗口号，并用 `CGWindowListCopyWindowInfo` 的 bounds 做几何匹配
- 修复：多 Space / 多显示器下焦点会跑到其他 Space。私有符号 `CGSSpaceCopyCurrent`、`CGSCopySpacesForWindow` 在 macOS 27 已不存在（dlsym 失败后 fail-open，过滤等于失效）。Space 过滤改由 `NSWindow.windowNumbers` 的「当前 Space」语义承担，不再使用任何私有 API
- 修复：Finder 桌面 / Quick Look 抑制在 macOS 27 失效。原实现依赖 `kCGWindowName`，而未授权屏幕录制时窗口名为空。改为判断 Finder 是否还存在标准窗口；同时给 AX 通知补上缺失的去抖，Finder 桌面元素被销毁重建时不再产生重复触发
- 修复：最小化后不聚焦下一个窗口。最小化与关闭在系统里表现完全不同：关闭会让窗口从 AX 窗口列表消失（~281ms），而最小化会**保留**该条目并打上「已最小化」标记；此前以「AX 列表里没有该窗口」作为判定条件，对最小化永远不成立。另外实测发现最小化动画期间目标应用的 AX 服务会停止响应，读取窗口列表会阻塞约 515ms（关闭时不会），因此最小化改为只做轻量的「屏幕列表」检查。窗口在整个 genie 动画期间都留在屏幕上（~660ms 才离开），提前聚焦会在动画中途抢走焦点，所以必须等它真正离开后再聚焦。实测最小化约 540ms，与动画结束时刻一致
- 优化：聚焦速度恢复到接近 v4.x 的手感。改用更早的判定信号 —— 窗口「已关闭/已隐藏」在辅助功能（AX）窗口列表里约 281ms 就反映出来，而在屏幕合成列表里要等到约 569ms；轮询间隔同时从 50ms 收紧到 25ms。实测端到端（判定→激活被接受）：`Cmd+W` 约 50ms（此前约 510ms），应用隐藏约 280ms（已贴近 281ms 的信号物理下限）
- 修复：关闭浏览器标签页等「窗口仍在」的操作会误抢焦点；最小化会在动画播完前就切走焦点。校验不能省：现在恢复「确认窗口真的消失」再聚焦
- 调整：应用隐藏的初始延迟改为 50ms（关闭/最小化仍为 0ms 立即检查）。隐藏的判定读的是应用的 AX 窗口数，而隐藏通知可能在该数值更新前就到达，留一点余量避免读到中间态。实测隐藏聚焦约 296–312ms（含该 50ms）
- 修复：最小化后有时不聚焦。两个原因：①判定等的是「窗口离开屏幕合成列表」，该信号慢且抖动（实测 Finder 725–940ms），会偶尔超过等待上限而被当成「没最小化」跳过；现改为读 `kAXMinimizedAttribute` 标记（实测 122–356ms 即翻转），稳定且更早。②`Cmd+M` 若没解析到窗口号，会掉进「应用隐藏」判定，而最小化**不改变** AX 窗口数，必然等到超时——现按触发类型分流，最小化不会再走隐藏判定
- 修复：`kAXWindowMiniaturizedNotification` 被误当成「应用隐藏」处理，携带的窗口号还是无关窗口，导致等待一个不会消失的窗口直到超时。最小化已由 `Cmd+M` 和黄灯点击覆盖，该通知不再监听
- 优化：应用隐藏的聚焦速度大幅提升。隐藏时窗口仍要等隐藏/关闭动画播完（约 400ms）才离开「屏幕合成列表」，但应用在约 140ms 时就已交出窗口、该信息直接体现在辅助功能（AX）窗口数上。隐藏路径改为只看 AX 窗口数变化：归零即聚焦（实测微信约 310ms、红点点击 45ms，此前约 280ms 或超时跳过）；窗口数减少但仍有窗口则立即判定「还有窗口」不再空等；窗口数不变（菜单/面板消失）继续等待，不会误触发
- 优化：真关窗的聚焦速度由轮询间隔决定（15ms），等待上限只用来兜住「窗口其实没消失」的情形，因此上限放宽到 1.0s 也不拖慢真关窗。实测 Finder 关窗约 134ms，最小化约 555ms（等动画结束），关标签页正确跳过
- 修复：应用隐藏（红点关闭 / 自带隐藏快捷键）后聚焦到了另一块屏的窗口。隐藏路径拿不到窗口号，显示器信息因此丢失，判定回退到全局 Z 序——于是「A 屏 Chrome 在最前、B 屏顺序为 微信>ZCode>Chrome」时关闭微信会跳到 B 屏的 Chrome 而非同屏的 ZCode。现在改为从源应用自身的窗口取显示器（隐藏时该窗口仍在屏幕上，约 400ms 后才消失）
- 新增：聚焦按「关闭窗口所在的显示器」收窄。关副屏的窗口就在副屏接着聚焦，不再跳到主屏；若该显示器没有可聚焦的窗口，回退到整个当前 Space。另外，激活应用会让它的窗口在所有显示器上一并置顶（这是 macOS 原生行为，真机验证过），会把非目标显示器上原本压着的窗口盖掉；现在会在激活settle后把其他显示器恢复原状
- 优化：关闭 / 最小化时若源应用还有 2 个及以上可见窗口，直接瞬间跳过判定——焦点本来就不需要移动，无需等待
- 加强：判定需连续两次轮询一致才动作，避免单次 AX 读取瞬时失败被误当成「窗口已消失」而抢焦点
- 修复：应用隐藏后不聚焦下一个窗口（微信/QQ/飞书等自带隐藏快捷键的场景）。隐藏时窗口同样需要约 350ms 才从「在屏」列表消失，而 AX 的窗口销毁通知约 130ms 就到了，原代码只做一次即时检查，因此每次都判定「应用仍有可见窗口」而跳过。改为等待该应用的窗口真正消失；如果窗口始终保留（如右键菜单消失但主窗口仍在），超时后跳过。该限制同时保证了「关闭多个窗口中的一个」不会误抢焦点
- 修复：关闭窗口后常常不聚焦下一个应用。实测 macOS 27 上关闭动画期间窗口会一直留在「在屏」列表中（至约 569ms），而 v4.x 只在触发后 50ms 检查一次，因此永远看不到窗口消失、每次都判定「未关闭」而跳过。改为按上述更早的信号判定；窗口确实保留（如关闭浏览器标签页）的场景仍会跳过
- 修复：激活操作可能被系统静默忽略。`activate(options: [.activateIgnoringOtherApps])` 中的该选项自 macOS 14 起就「不会生效」，且原代码丢弃了返回值。改为按序尝试「AX frontmost → activate(from:options:) → activate(options: [.activateAllWindows])」，逐步校验并记录结果
- 改进：关键链路日志从 `info` 改为 `notice` 并写盘持久化。原先 `log show` 默认查不到、且只保留几分钟，出故障无法回溯。启动时输出辅助功能 / 输入监控 / 屏幕录制授权状态，AXObserver 注册失败不再静默
- 改进：签名改用本地证书（`Focus TrafficLight Local Signing`）。此前 ad-hoc 签名把辅助功能授权绑定在二进制哈希上，每次重新编译都会被 macOS 27 撤销、需重新授权
- 要求：最低系统版本提升至 macOS 27.0

## v4.0.6 - 2026-09-11

- 修复：Finder 关闭最后一个窗口后，不聚焦下一个置顶应用
- 收窄 Finder 的 AX 抑制：只压制桌面 / Quick Look 产生的窗口销毁、最小化事件；真正的 Finder 关窗 / 最小化事件正常进入聚焦恢复
- 保持既有行为：桌面 Quick Look 预览不再触发误聚焦


## v4.0.5 - 2026-09-11

- 修复：Chrome 下载浮层打开文件时，Archive Utility 等临时系统工具销毁窗口被误判为应用隐藏，导致焦点被抢回 Chrome
- 跳过 Archive Utility、Disk Image Mounter 的 AX 窗口通知，不再对临时系统工具执行焦点恢复
- 保留微信/QQ、飞书隐藏，`Cmd+W` / `Cmd+M`、红黄点点击和桌面 Quick Look 的现有行为

## v4.0.4 - 2026-09-01

- 修复：右键菜单、框选等临时 UI 消失被误判为应用隐藏，导致焦点被抢到其他窗口
- `kAXApplicationHiddenNotification` 只有在应用真实处于隐藏状态时才触发焦点恢复
- 隐藏通知后若源应用仍有可见窗口，直接跳过，不再抢占焦点
- 保留微信/QQ、飞书等自定义隐藏快捷键的聚焦行为

## v4.0.3 - 2026-08-28

- 修复：桌面 Quick Look（Finder 空格预览）触发焦点误切换
- Finder 前台时的 AX 销毁/最小化通知直接跳过，Quick Look 打开/关闭不再触发焦点恢复
- 不影响 `Cmd+W` / `Cmd+M`、红绿灯点击和微信隐藏等正常触发路径

## v4.0.2 - 2026-08-25

- 优化：触发后 50ms 只做一次目标窗口检查，不再按 50ms 间隔轮询最长 1 秒
- 关闭标签页等窗口仍在的场景不再白等，连续关闭/最小化操作响应更快
- 移除 1 秒超时与轮询逻辑，窗口消失则聚焦下一个，未消失则直接跳过

## v4.0.1 - 2026-08-25

- 修复：v2rayN 等 `activationPolicy` 为 accessory、但拥有真实窗口的应用无法被聚焦
- 简化：候选窗口只按 `CGWindowList` 的 `layer 0` + 当前 Space + 非自身进程筛选
- 移除尺寸、激活策略等启发式判断，Keynote 保存面板等场景保持可用

## v4.1.0

- 新增：微信自身隐藏（窗口隐藏通知）后，自动聚焦下一个最顶层可见窗口
- 保留并优化已有的 `Cmd+W` / `Cmd+M` 焦点恢复逻辑

## v4.0.0 - 2026-08-24

- 基础的 `Cmd+W` / `Cmd+M` 可正常使用
- 重写触发逻辑：只响应明确快捷键，不再监听通用 AX 窗口生命周期事件
- 关闭/最小化最后一个窗口后，聚焦当前 Space 最顶层的有效窗口
