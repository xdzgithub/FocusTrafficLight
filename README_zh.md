[English](./README.md) | 中文

# FocusTrafficLight

macOS 菜单栏应用，自动管理窗口焦点恢复。

## 功能

- **键盘触发**: 监听用户主动的 `Cmd+W`（关闭窗口）和 `Cmd+M`（最小化窗口）
- **红绿灯点击**: 识别点击关闭红点、最小化黄点后自动转移焦点
- **应用隐藏聚焦**: 微信、QQ、飞书等自带隐藏快捷键的应用，窗口隐藏后自动聚焦下一个窗口
- **AX 关闭/最小化兜底**: 窗口销毁、最小化通知只在事件应用为当前前台时采用，后台应用无法抢焦点
- **单次 50ms 检查**: 触发后只检查一次目标窗口；窗口仍在屏幕上（例如关闭浏览器标签页）时直接跳过，不做轮询
- **最顶层可见窗口选择**: 目标窗口消失后，聚焦当前 Space 第一个 `layer 0` 窗口，排除本应用自身窗口
- **兼容非标准应用**: 不依赖 AX 角色、尺寸、激活策略等启发式判断，v2rayN、Keynote 保存面板都能正常工作
- **空闲几乎不占 CPU**: 所有监听都是事件驱动，没有后台轮询
- **菜单栏控制**: 简洁的菜单栏界面，一键开关

## 工作流程

1. 检测到显式动作：`Cmd+W`、`Cmd+M`、红绿灯点击或应用隐藏。
2. 从键盘事件、AX 聚焦窗口或 AX 元素中取得目标窗口的 `CGWindowID`。
3. 50ms 后对照 `CGWindowList(.optionOnScreenOnly)` 检查一次。
4. 窗口已消失 → 激活当前 Space 最顶层的可见窗口；窗口仍在 → 不做任何事。

额外防护：

- 记录 `Cmd+H`，之后 0.5 秒内到达的隐藏通知会被忽略，避免和系统自身的隐藏行为重复。
- 0.2 秒防抖合并快速重复触发，忽略长按自动重复的按键。
- AX 销毁/最小化通知若来自后台应用，按 PID 过滤，不会触发聚焦。

## 设计说明

- 只有用户主动关闭、最小化或隐藏窗口，或前台应用的窗口销毁/最小化事件，才会转移焦点
- 候选窗口直接按 `CGWindowList` 的 z-order 选择：`layer 0`、当前 Space、非本进程
- 不依赖 AX 窗口角色匹配，因此 Keynote 保存面板、v2rayN 这类窗口都能正常工作
- 触发后 50ms 只做一次检查，不做轮询等待；拿不到目标窗口 ID 时按已消失处理

## 系统要求

- macOS（已在 macOS 15 测试，其他版本自测）
- 需要辅助功能权限

## 安装

1. 从 Releases 下载最新的 `FocusTrafficLight_vX.Y.Z.zip`
2. 解压并拖入应用程序文件夹
3. 首次启动时授予辅助功能权限，**授权后需重启应用**

## 使用

- 点击菜单栏图标查看状态
- "Enable Focus" 开关控制是否启用焦点恢复
- "Launch at Login" 设置开机自启

## 隐私

本应用需要辅助功能权限来读取窗口状态。权限仅用于焦点管理，不会收集或传输任何用户数据。
